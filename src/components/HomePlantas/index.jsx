import styled from 'styled-components';

const HomePlantas = () => {
    return (  
        <Container>
        </Container>
    );
}

const Container = styled.div`
    width: 100%;
    height: 1000px;
`;

export default HomePlantas;